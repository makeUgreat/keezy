apiVersion: v1
kind: Service
metadata:
  name: {{ include "keezy.fullname" . }}
  labels:
    {{- include "keezy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keezy.selectorLabels" . | nindent 4 }}
